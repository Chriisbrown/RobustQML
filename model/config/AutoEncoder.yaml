model: AutoEncoderModel
run_config :
  verbose : 2
  debug : True
  num_threads : 64

model_config :
  name : autoencoder

training_config :
  epochs : 100
  batch_size : 1024
  learning_rate : 0.001
  validation_split : 0.1